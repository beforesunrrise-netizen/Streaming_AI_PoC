# Streamlit Cloud Secrets Configuration
# 이 파일은 로컬 테스트용입니다.
# 실제 배포 시에는 Streamlit Cloud 웹사이트에서 Secrets를 설정해야 합니다.

# LLM Usage (true/false)
USE_LLM = "true"

# OpenAI API Key (실제 배포 시 Streamlit Cloud에서 설정)
OPENAI_API_KEY = "your_openai_api_key_here"
